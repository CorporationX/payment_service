server:
  port: 9080

app:
  commission: 1.01
  default_currency: USD
  divide_scale: 7

logging:
  level:
    root: info

services:
  open_exchange:
    host: https://openexchangerates.org
    app_id: ${OPENEXCHANGERATES_TOKEN}