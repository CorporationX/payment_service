server:
  port: 9080

logging:
  level:
    root: info

spring:
    data:
      redis:
        port: 6379
        host: localhost
        channels:
          mentorship-channel:
            name: mentorship_channel
          follower-event-channel:
            name: follower_event