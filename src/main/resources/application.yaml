spring:
  config:
    import: optional:classpath:application.properties

  jackson:
    default-property-inclusion: non_null

  profiles:
    active: ${APP_ENV}

server:
  port: 9080

logging:
  level:
    root: info