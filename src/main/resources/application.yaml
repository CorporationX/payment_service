spring:
  profiles:
    active: local


server:
  port: 9080

logging:
  level:
    root: info

---

spring:
  config:
    activate:
      on-profile: local

logging:
  level:
    root: debug

---

spring:
  config:
    activate:
      on-profile: prod

logging:
  level:
    root: info
